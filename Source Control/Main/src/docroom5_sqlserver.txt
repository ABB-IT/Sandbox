create table DM_SOURCE (
  SOURCEID     int NOT NULL,
  SOURCE       VARCHAR(80) NOT NULL,
  DOCTYPEID    VARCHAR(4) NULL,
  LANGCODE     VARCHAR(5) NULL);

create table DM_TOPIC (
  TOPICID      int NOT NULL,
  TOPIC        VARCHAR(80) NOT NULL);

create table DM_LANG (
  LANGID       int NOT NULL,
  LANGCODE     VARCHAR(5) NOT NULL,
  LANGENGLISH  VARCHAR(50) NULL,
  LANGFRENCH   VARCHAR(50) NULL,
  LANGDUTCH    VARCHAR(50) NULL,
  LANGICON     VARCHAR(20) NULL);

create table DM_UDC (
  UDCID        int NOT NULL,
  UDCCODE      VARCHAR(100) NOT NULL,
  DESCR_DUTCH  VARCHAR(1000) NULL,
  DESCR_FRENCH VARCHAR(1000) NULL);

insert into RTSEQUENCES (SEQ_ID,SEQ_SITE,SEQ_NAME,SEQ_NUMBER) values ( 1,'CENTRAL','SEQ_DM_TOPIC',1000);
insert into RTSEQUENCES (SEQ_ID,SEQ_SITE,SEQ_NAME,SEQ_NUMBER) values ( 1,'CENTRAL','SEQ_DM_SOURCE',1000);
insert into RTSEQUENCES (SEQ_ID,SEQ_SITE,SEQ_NAME,SEQ_NUMBER) values ( 1,'CENTRAL','SEQ_DM_LANG',1000);
insert into RTSEQUENCES (SEQ_ID,SEQ_SITE,SEQ_NAME,SEQ_NUMBER) values ( 1,'CENTRAL','SEQ_DM_UDC',1000);

Eventueel DM_UDC1, DM_UDC2 etc. voorzien
Eventueel SEQ_DM_UDC1, SEQ_DM_UDC2 etc. voorzien

